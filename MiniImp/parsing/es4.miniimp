// Nested conditionals and parentheses

// Example tests:
// input x = 12, output y = 10
// input x = -5, output y = 0
// input x = 7, output y = 7

def main with input x output y as (
  if x > 10
  then (y := 10)
  else (
    if x < 0
    then (y := 0)
    else (y := x)
  )
)
