// While loop with x decrement and y increasement

// Example tests:
// input x = 3, output y = 3
// input x = 0, output y = 0
// input x = -1, output y = 0

def main with input x output y as (
  y := 0;
  while x > 0 do (
    y := y + 1;
    x := x - 1
  )
)
